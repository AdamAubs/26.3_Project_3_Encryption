#ifndef ENCRYPTIONMENU
#define ENCRYPTIONMENU

#include <string>

struct EncryptMenu
{
	int choice{};
	int shift{ };
	char direction{};
	/*W message{ "And, now, for the backwards choice" };*/
	//std::string message{};
	std::string message{};
	/*W message{ "IFMMP" };*/
};

#endif // !1

